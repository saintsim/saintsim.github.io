name: Update BRT timetable

# Re-scrapes the Tokyo BRT timetables weekly (Sunday morning JST, leaving the day to fix a
# failed run) and opens a PR when they change. Run it sooner from the Actions tab with "Run workflow".
# Needs Settings -> Actions -> General -> "Allow GitHub Actions to create and approve pull requests".
# GitHub disables scheduled workflows after 60 days without repo activity; re-enable it from the Actions tab.
on:
  schedule:
    # Sunday 00:23 UTC = 09:23 JST (GitHub cron is always UTC; off the hour, as :00 runs are often delayed)
    - cron: '23 0 * * 0'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Test scraper against saved pages
        run: npm test

      - name: Scrape timetables
        run: node scripts/scrape-brt.mjs

      - name: Check for changes
        id: diff
        run: |
          if git diff --quiet -- src/bus-schedule.ts; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            { echo '```diff'; git diff -- src/bus-schedule.ts; echo '```'; } >> "$GITHUB_STEP_SUMMARY"
          fi

      # The site is built from the compiled built/*.js, so recompile the schedule too.
      - name: Compile
        if: steps.diff.outputs.changed == 'true'
        run: |
          npm install
          npx tsc

      - name: Open pull request
        if: steps.diff.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          branch: brt-timetable-update
          add-paths: |
            src/bus-schedule.ts
            built/bus-schedule.js
          commit-message: Update BRT timetable
          title: Update BRT timetable
          body: |
            The Tokyo BRT timetable changed. Check the times against the stop pages before merging:
            - https://www.tokyo-brt.co.jp/bus-stops/b02-kachidoki-brt (Toranomon Hills-bound only)
            - https://www.tokyo-brt.co.jp/bus-stops/b11-toranomon-hills (all departures)

            Merging deploys the site.
